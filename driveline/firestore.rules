rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All client access denied — DivineBytes Hub uses Express Admin SDK only.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
